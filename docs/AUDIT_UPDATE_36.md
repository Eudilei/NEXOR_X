# Update 36 — Inicialização em um comando + Cloudflared

O comando `nexor-x` passa a iniciar o NEXOR X, painel web, Scheduler,
Ollama local quando instalado e Cloudflared quando instalado.

O painel local continua disponível mesmo se Ollama ou Cloudflared não estiverem
instalados. LIVE permanece bloqueado.
